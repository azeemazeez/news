{
  "name": "news-portal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "fetch": "node --env-file=.env scripts/fetch-news.js",
    "dev": "npx serve public"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
